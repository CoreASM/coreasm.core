CoreASM Time1

use Standard
use Time

init Start

/*
 * @minsteps 1
 * @maxsteps 3
 *
 * @require "Time:\n"
 * @require "Computation step: 2\n"
 */
 // Running this with the coreASM plugin gives the correct output after a maximum of 3 steps. Note that
 // the failure occurs because the TestDriver evaluates the whole required output already after the first step.
rule Start =
	seqblock
		print "Time:"
		print now
		print "Computation step: "+stepcount
	endseqblock